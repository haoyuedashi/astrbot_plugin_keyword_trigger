{
    "keywords": {
        "description": "关键词触发列表",
        "hint": "输入后自动添加 / 前缀触发对应指令",
        "type": "list",
        "default": [
            "打工",
            "宝宝菜单"
        ],
        "items": {
            "type": "string"
        }
    },
    "enable_group_only": {
        "description": "仅群聊触发",
        "hint": "私聊不触发",
        "type": "bool",
        "default": true
    }
}